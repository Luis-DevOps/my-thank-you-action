name: 'Than You Action'
description: 'Say "Thank You" to new pull request'
author: 'Luis Castillo'

inputs:
  GITHUB_TOKEN:
    description: 'Github token'
    required: true
  TENOR_TOKEN:
    description: 'Tenor API token'
    required: true

runs:
  using: node12
  main: dist/index.js